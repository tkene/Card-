{
  "name": "card-game",
  "version": "1.0.0",
  "description": "Card game project with Tailwind CSS",
  "scripts": {
    "build-css": "tailwindcss -i ./assets/styles/app.css -o ./public/build/app.css --minify",
    "start": "php -S localhost:8000 -t public"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}
