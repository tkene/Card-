{% extends 'base.html.twig' %}

{% block title %}Étape 3 : Choisir le nombre de cartes{% endblock %}

{% block body %}
<div class="bg-white rounded-[20px] shadow-[0_20px_60px_rgba(0,0,0,0.3)] p-10 max-w-[700px] w-full text-center">
    <h1 class="text-text-dark text-[2em] mb-2.5 drop-shadow-[2px_2px_4px_rgba(0,0,0,0.1)]">
        Étape 3 : Choisir le nombre de cartes 🃏
    </h1>
    <p class="text-text-medium text-[1.1em] mb-10">
        Combien de cartes veux-tu tirer ?
    </p>

    {% if app.flashes('error') %}
        {% for message in app.flashes('error') %}
            <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-5">
                {{ message }}
            </div>
        {% endfor %}
    {% endif %}

    <form method="POST" action="{{ path('app_confirm_cards_number') }}" class="flex flex-col gap-5 my-10">
        <div class="flex justify-center">
            <div class="bg-gradient-item p-6 rounded-[15px] shadow-[0_4px_10px_rgba(0,0,0,0.1)] flex items-center justify-center gap-3">
                <div class="text-[3em]">🃏</div>
                <input 
                    type="number" 
                    name="numberOfCards" 
                    id="numberOfCards"
                    min="1" 
                    max="52" 
                    value=""
                    required
                    class="text-center text-[2em] font-bold text-text-dark bg-transparent border-none outline-none w-24 focus:ring-2 focus:ring-primary-from rounded-lg"
                    placeholder="7"
                >
                <span class="text-text-medium text-[1.1em]">cartes</span>
            </div>
        </div>

        <div class="flex gap-4 justify-center mt-5">
            <button 
                type="submit"
                class="bg-gradient-primary text-white px-12 py-4 text-[1.2em] font-bold rounded-[50px] cursor-pointer transition-all duration-300 ease-in-out shadow-[0_4px_15px_rgba(102,126,234,0.4)] inline-block hover:-translate-y-0.5 hover:shadow-[0_6px_20px_rgba(102,126,234,0.6)]"
            >
                Confirmer
            </button>
        </div>
    </form>
</div>
{% endblock %}
