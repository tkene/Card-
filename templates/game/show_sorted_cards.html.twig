{% extends 'base.html.twig' %}

{% block title %}Étape 5 : Main triée{% endblock %}

{% block body %}
<div class="w-full min-h-screen flex items-center justify-center p-5">
    <div class="bg-white rounded-[20px] shadow-[0_20px_60px_rgba(0,0,0,0.3)] p-10 w-full max-w-[95vw] max-h-[95vh] overflow-auto">
        <div class="text-center mb-10">
            <h1 class="text-text-dark text-[2em] mb-2.5 drop-shadow-[2px_2px_4px_rgba(0,0,0,0.1)]">
                Étape 5 : Main triée 📏
            </h1>
            <p class="text-text-medium text-[1.1em] mb-5">
                Les cartes sont maintenant triées selon les règles :
            </p>
            <div class="bg-gray-50 rounded-[15px] p-4 mb-5 border-2 border-gray-200">
                <p class="text-text-dark text-[1em] mb-2">
                    <strong>1.</strong> D'abord par couleur, selon l'ordre choisi à l'étape 1
                </p>
                <p class="text-text-dark text-[1em] mb-2">
                    <strong>2.</strong> Ensuite par valeur, selon l'ordre choisi à l'étape 2
                </p>
            </div>
        </div>

        <div class="w-full pb-5">
            <div class="bg-gray-50 rounded-[15px] p-6 border-2 border-gray-200 overflow-hidden w-full" style="box-sizing: border-box;">
                <div class="grid grid-cols-5 gap-4 auto-rows-fr">
                    {% for card in sortedCards %}
                    <div class="bg-gradient-item p-6 rounded-[15px] flex flex-col items-center justify-center gap-3 shadow-[0_4px_10px_rgba(0,0,0,0.1)] transition-transform duration-300 ease-in-out hover:-translate-y-1 hover:shadow-[0_6px_15px_rgba(0,0,0,0.15)] w-full">
                        <div class="text-[3em]">
                            {{ card.symbol }}
                        </div>
                        <div class="text-[1.1em] font-bold text-text-dark text-center">
                            {{ card.value }}
                        </div>
                        <div class="text-[1em] text-text-medium text-center">
                            {{ card.color }}
                        </div>
                    </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <div class="flex gap-4 justify-center mt-10">
            <a 
              href="{{ path('app_show_cards_with_values') }}"
              class="bg-gradient-primary text-white px-12 py-4 text-[1.2em] font-bold rounded-[50px] cursor-pointer transition-all duration-300 ease-in-out shadow-[0_4px_15px_rgba(102,126,234,0.4)] inline-block hover:-translate-y-0.5 hover:shadow-[0_6px_20px_rgba(102,126,234,0.6)]"
            >
                Retour vers la main non triée
            </a>
            <a 
              href="{{ path('app_reset_game') }}"
              class="bg-gradient-primary text-white px-12 py-4 text-[1.2em] font-bold rounded-[50px] cursor-pointer transition-all duration-300 ease-in-out shadow-[0_4px_15px_rgba(102,126,234,0.4)] inline-block hover:-translate-y-0.5 hover:shadow-[0_6px_20px_rgba(102,126,234,0.6)]"
            >
                Reseter la partie
            </a>
        </div>
    </div>
</div>
{% endblock %}
